---
name: xiaoxi-comment-login-logic
description: 设计、实现、审查或排查无密码评论登录系统，涵盖友情链接身份、普通邮箱验证码、服务端设备信任令牌、登录安全邮件、设备管理和用户主动登录弹窗。适用于替换第三方评论服务、设计评论身份认证、阻止清缓存绕过授权、增加可信设备管理，或将评论身份与网站友情链接目录结合的场景。
---

# 小曦的园子评论登录逻辑

使用本 Skill 设计一种低打扰、可撤销的评论区身份系统。评论系统应独立于 Github 等第三方评论平台运行，并将友情链接、邮箱验证、浏览器会话、设备信任、安全邮件和评论管理视为一套完整系统。

## 核心目标

- 普通用户无需注册密码即可发表评论。
- 友情链接站长使用网站 Logo、网站名称和网站地址作为评论身份。
- 未加入友情链接的用户使用短时有效的邮箱验证码登录。
- 友情链接用户首次登录后，在可信设备上尽量无感恢复登录。
- 所有信任状态都可以通过邮件、设备列表、后台重置或服务端撤销。
- 不把浏览器指纹、IP、User-Agent、Canvas 指纹或随机客户端 UUID 当作身份凭证。
- 页面加载、令牌失效或用户退出后，都不能自动打开登录弹窗。

## 身份数据模型

无论使用何种框架或数据库，都应区分以下概念：

1. **评论会话**：当前浏览器用于读取、发表评论、回复和点赞的凭证。
2. **信任设备**：由服务端生成的高强度随机 token，绑定邮箱和友情链接。服务端只保存 token 摘要，并保存创建时间、最近使用时间、设备名称、IP、User-Agent 和撤销时间。
3. **友情链接身份**：网站名称、网址、Logo 和站长邮箱。邮箱匹配前必须统一大小写并规范化。
4. **邮箱登录验证**：普通邮箱用户使用的一次性验证码记录，包含有效期、尝试次数和发送冷却时间。
5. **安全操作记录**：登录提醒或评论提醒中的一次性拒绝 token。服务端只保存摘要，并设置有效期。

不要使用邮箱、评论查看链接或浏览器指纹替代可撤销的设备 token。评论历史与登录状态应分开保存，退出登录或移除设备时不能误删合法历史评论。

## 登录流程

### 一、恢复已有会话

页面加载时：

- 读取当前评论会话 token；
- token 有效时直接恢复身份，不打开弹窗；
- token 无效时静默清除；
- 读取设备信任 token，仅用于尝试静默恢复；
- 静默恢复失败时保持未登录，不弹窗，也不显示内部令牌错误。

用户主动点击退出登录时，写入一个本地“手动退出”标记。标记存在期间，刷新页面不能自动恢复设备信任状态。只有用户再次主动完成登录后，才能清除这个标记。

### 二、处理主动登录

登录弹窗只能由明确的用户操作打开，例如点击“点击登录”。不要因为页面加载、令牌失效或其他后台请求自动打开弹窗。

用户输入邮箱后，使用防抖方式进行身份查询：

- 如果邮箱属于友情链接，显示对应网站 Logo、名称，并显示“以该网站身份登录评论区”；
- 如果邮箱不属于友情链接，显示普通邮箱验证码流程，并说明该邮箱尚未加入友情链接。

### 三、友情链接登录

服务端按照以下顺序判断：

- 传入有效设备信任 token：更新最近使用信息并直接登录；
- 传入的 token 已撤销：拒绝登录，转入邮箱授权；
- 没有 token，且该邮箱从未出现过设备信任记录：允许首次免授权登录，并创建设备信任 token；
- 没有 token，但该邮箱曾经存在任何历史设备信任记录：必须通过邮箱授权。这样可以防止更换浏览器或清空缓存后再次绕过授权；
- 邮箱授权成功后，创建当前评论会话，并按策略创建新的设备信任记录。

必须保留撤销设备的历史记录，不能因为删除记录而让邮箱重新变成“首次登录”。

### 四、普通邮箱登录

对于没有友情链接的邮箱：

- 发送短时有效的数字验证码；
- 限制重复发送、验证码有效期、尝试次数和邮箱/IP 频率；
- 验证成功后立即消耗验证码；
- 创建普通邮箱评论身份，并采用隐私友好的昵称和头像展示规则。

除非产品明确要求，不要让友情链接邮箱绕过友情链接身份进入普通验证码流程。

## 邮件安全设计

每次友情链接登录成功后都发送登录安全提醒，包括首次免授权登录和之后的设备信任登录。邮件应包含网站名称、友情链接身份、登录时间、设备名称和登录 IP。

邮件中明确说明：

- 如果是本人登录，可以忽略邮件；
- 如果不是本人登录，可以点击“不是我”；
- 点击后撤销对应设备 token，并使相关评论会话失效。

发表评论后可以发送独立的评论身份确认邮件。如果收件人确认不是本人操作，就删除目标评论，并按照站点策略撤销该邮箱相关的评论凭证。正常用户不应因为每条评论都必须点击确认而被打断，邮件主要承担异常撤销和安全恢复作用。

安全链接必须满足以下条件：一次性、不可猜测、随机或签名、服务端只存摘要、具有有效期，并且重复访问时不会泄露敏感信息。

## 设备管理

在用户的评论查看页面提供“当前登录设备”列表，至少显示：

- 便于理解的设备名称；
- 最近登录时间；
- 最近使用的 IP；
- 明确的移除按钮。

移除设备时，将设备 token 标记为撤销，并使对应评论会话失效。如果系统依靠历史记录阻止清缓存绕过授权，不要删除设备历史记录。设备列表和移除接口必须通过评论访问 token 验证友情链接用户身份，不能被后台用户组权限中间件误拦截。

## 退出登录行为

严格区分“退出当前会话”和“撤销设备”。

手动退出登录应当：

- 删除浏览器中的当前评论会话；
- 写入手动退出标记，避免刷新后立即静默登录；
- 保留设备信任 token；
- 用户下次主动点击登录时，不要求重新邮箱授权。

移除设备、登录安全邮件中点击“不是我”、友情链接评论链接重置或后台安全操作，才应撤销设备信任 token。这些操作的强度高于普通退出登录。

## 隐私和安全边界

- 邮箱查询统一规范化，但公开评论中不要无必要地展示完整邮箱；
- token 必须由服务端使用高强度随机源生成，数据库只保存摘要；
- 禁止使用 `Math.random()`、32 位字符串哈希、Canvas 指纹、字体列表、WebGL 渲染器或屏幕尺寸作为身份凭证；
- IP 和 User-Agent 只作为审计信息，不作为身份认证因素；
- 对验证码、登录、发表评论和安全操作增加频率限制；
- 谨慎处理“邮箱是否属于友情链接”的信息泄露；
- 每次受保护的评论操作都必须重新验证 token，不能只在页面加载时验证一次。

## 前台交互要求

- 使用站点已有的弹窗样式，不另造一套不一致的 UI；
- 只有用户主动点击登录时才打开弹窗；
- 登录进行中不要因为误触遮罩层而关闭弹窗；
- 登录和发表评论时，只虚化当前操作区域并显示加载动画；
- 适配浅色和暗色主题、移动端键盘、焦点稳定性和无障碍标签；
- 登录成功后可短暂显示头像和昵称，再关闭弹窗；
- 静默登录失败后保持未登录，不自动弹窗，不展示内部 token 错误。

## 实施顺序

1. 先检查评论 API、浏览器存储键、数据库表、弹窗组件、主题选择器和权限中间件。
2. 先画状态机，覆盖匿名、首次友情链接登录、可信设备恢复、失效 token 授权、普通邮箱登录、手动退出和安全撤销。
3. 增加数据库迁移，但不要删除历史信任记录。
4. 先实现服务端 token 校验和撤销，再实现前端恢复逻辑。
5. 增加邮件模板和一次性安全操作链接。
6. 通过公开的已认证评论接口实现设备列表和移除，不要误接后台管理权限。
7. 至少测试以下场景：
   - 首次友情链接登录；
   - 有效设备 token 刷新恢复；
   - 手动退出后刷新页面；
   - 手动退出后主动点击登录；
   - 清空浏览器存储或更换浏览器；
   - 移除设备后的旧 token；
   - 邮件点击“不是我”；
   - 普通邮箱验证码冷却和过期；
   - 暗色主题和移动端输入。
8. 最后执行后端语法检查、前端构建检查和受保护接口冒烟测试。

迁移到其他技术栈时，保留上述行为约束即可，不要机械复制具体接口名称或框架实现。
